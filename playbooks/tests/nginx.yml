---

- name: test | nginx is installed
  shell: nginx -v
  tags: test

- name: test | nginx is properly configured
  local_action: "command curl -I static.{{ domain }}:{{ http_port }}/favicon.ico"
  register: curl_result
  failed_when: "'HTTP/1.1 200 OK' not in curl_result.stdout"
  tags: test
